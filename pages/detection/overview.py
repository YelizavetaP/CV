import streamlit as st

st.title("📦 Object Detection")
st.markdown(
    """
    **Topics:** sliding window, anchor boxes, IoU, NMS.
    Architectures: R-CNN → Fast R-CNN → Faster R-CNN, SSD, YOLO (v1 → v8),
    DETR (detection transformer).
    """
)

st.info("Add your experiments and demos here.")
